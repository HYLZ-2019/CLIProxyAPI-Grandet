# Version Design

用于记录用户对产品最终形态的原始想法。

---

## 更好的额度与用量记录统计分析功能

### API 秘钥机制

现在是只有一个 list 的 API 秘钥。希望给每个 API 秘钥：

1. 一个唯一的 id（1/2/3/4/....），删掉前面的 key 之后，后面 key 的唯一 id 不会变，如果新建一个 key，会往后面编号，而不是替代前面的编号。

2. 一个 string，是它的名称，可以修改和编辑，例如 "alice-work"。
